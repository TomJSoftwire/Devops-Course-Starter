version: "3"
services:
  todo-app:
    container_name: todo-app-test
    image: todo-app:test
    build:
      context: .
      target: test
      dockerfile: Dockerfile
    # This command line breaks pytest for some reason 
    command: tests

    env_file:
      - ".env.test"